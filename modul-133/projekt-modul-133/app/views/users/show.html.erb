<div class="container" style="margin-top: 100px">
    <h1>Userlist</h1>

    <p>Hier sind die User aufgelistet</p>
    <p>Anzahl Datensätze: <%= @users.count %></p>

    <br>

    <ol>
        <% @users.each do |u| %>
            <li>
                <ul>
                    <%=u.name%>
                </ul>
                <ul>
                    Email: <a href="mailto:<%=u.email%>"><%=u.email%></a>
                </ul>
                <ul>
                    Alter: <%=u.age%>
                </ul>
            </li>
        <% end %>
    </ol>
</div>